# Rust
target

# Local tooling state
.firecrawl
.claude/settings.local.json
.claude/worktrees/
.worktrees/
.env

# Local planning notes (not shipped)
/PLAN.md

# Editor and OS
.idea
.DS_Store

# Local contributor/agent instructions (not shipped)
AGENTS.md

# Plan-mode runtime state (written by the plan-mode hooks, never shipped)
.openmax/state/
